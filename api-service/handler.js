const uploadImage = (req, res, next) => {
  res.status(200).send()
}

const getURL = (req, res, next) => {
  res.send('')
}

const checkStatus = (req, res, next) => {
  res.send('')
}

module.exports = {
  uploadImage,
  getURL,
  checkStatus
}